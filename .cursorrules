# Quest Manager - AI Assistant Rules

## 🎯 Основные принципы работы

### 1. Анализ перед изменением
- ВСЕГДА начинать с семантического поиска по кодовой базе
- Понять архитектуру и связи между компонентами
- Найти все связанные места перед внесением изменений
- Проверить тесты и их покрытие

### 2. Иерархия поиска информации
```
1. codebase_search - семантический поиск по коду
2. grep - точный поиск по паттернам
3. read_file - чтение конкретных файлов
4. web_search - только для внешних зависимостей
```

### 3. Приоритеты изменений
```
1. БЕЗОПАСНОСТЬ (data races, memory leaks, concurrency issues)
2. ФУНКЦИОНАЛЬНОСТЬ (баги, новые фичи)
3. ПРОИЗВОДИТЕЛЬНОСТЬ
4. ЧИТАЕМОСТЬ КОДА
5. ТЕСТИРУЕМОСТЬ
```

## 🏗️ Архитектурные правила

### Domain-Driven Design (DDD)
- Domain layer НЕ должен зависеть от infrastructure
- Business logic только в domain models
- Use cases координируют между domain и infrastructure
- События публикуются после успешного commit

### Clean Architecture
- Внешние зависимости направлены внутрь
- Интерфейсы в ports, реализации в adapters
- Каждый слой имеет свою ответственность

### Concurrency Safety
- UnitOfWork должен быть per-request (НЕ shared между запросами)
- EventPublisher должен быть thread-safe
- Репозитории должны работать в рамках транзакций

## 🔧 Правила работы с кодом

### Перед изменением:
1. Найти все связанные компоненты
2. Понять текущую архитектуру
3. Проверить существующие тесты
4. Убедиться в совместимости

### При изменении:
1. Минимальные изменения для достижения цели
2. Сохранять обратную совместимость
3. Обновлять тесты вместе с кодом
4. Следовать существующим паттернам

### После изменения:
1. Проверить компиляцию: `go build ./...`
2. Запустить тесты: `go test ./...`
3. Проверить линтер: `golangci-lint run`
4. Запустить интеграционные тесты

## 🧪 Правила тестирования

### Типы тестов:
- **Domain tests** - бизнес-логика без внешних зависимостей
- **Contract tests** - интерфейсы между слоями
- **Integration tests** - полный цикл с базой данных
- **HTTP tests** - API endpoints

### Изоляция тестов:
- Каждый тест должен быть независимым
- Использовать `-p 1` для интеграционных тестов
- Очищать базу данных между тестами
- Использовать моки для внешних зависимостей

## 📝 Правила форматирования

### Go код:
- Использовать `goimports` для импортов
- Следовать `golangci-lint` правилам
- Комментарии на английском языке
- Документация для публичных API

### Структура файлов:
- Один тип на файл для больших структур
- Группировать связанные функции
- Логическое разделение по слоям

## 🚨 Критические проблемы

### UnitOfWork Concurrency Issue
**ПРОБЛЕМА:** Один UnitOfWork шарится между HTTP запросами, что приводит к data races.

**РЕШЕНИЕ:** 
- Создавать новый UnitOfWork для каждого запроса
- Использовать UnitOfWorkFactory
- EventPublisher должен создавать свой tracker

### Authentication Middleware
- Все endpoints требуют JWT аутентификации
- Использовать mock для тестов
- Обрабатывать ошибки через ProblemDetails

## 🎯 Правила приоритета контекста

### 1. **Пользовательский контекст - ВЫСШИЙ ПРИОРИТЕТ**
```
Если пользователь указал конкретный контекст:
- ВСЕГДА начинать с указанного пользователем контекста
- Использовать его как основу для анализа
- Дополнять семантическим поиском только при необходимости
```

### 2. **Иерархия поиска (когда контекст не указан):**
```
1. codebase_search - семантический поиск по коду
2. grep - точный поиск по паттернам  
3. read_file - чтение конкретных файлов
4. web_search - только для внешних зависимостей
```

### 3. **Примеры указания контекста:**
```
✅ "В файле cmd/router.go исправь проблему с..."
✅ "Согласно authentication.go, сделай аналогично в..."
✅ "Исходя из структуры CompositionRoot, добавь..."
✅ "По аналогии с UnitOfWork, создай..."
```

### 4. **Алгоритм работы:**
```
1. Проверить - указал ли пользователь конкретный контекст?
2. ЕСЛИ ДА → начать с указанного контекста
3. ЕСЛИ НЕТ → использовать стандартную иерархию поиска
4. Дополнить анализом связанных компонентов
```

### 5. **Типы контекста:**
```
📁 Файловый контекст: "В файле X..."
🏗️ Архитектурный контекст: "Согласно структуре Y..."
🔄 Паттерн контекст: "По аналогии с Z..."
📋 Функциональный контекст: "Исходя из логики A..."
```

### 6. **Обязательные действия при указанном контексте:**
```
1. СНАЧАЛА прочитать указанный файл/компонент
2. Понять структуру и паттерны
3. Применить найденные паттерны к задаче
4. ТОЛЬКО ПОТОМ искать связанные компоненты
5. Убедиться в консистентности с указанным контекстом
```

## 🔍 Примеры правильных запросов

### С указанием контекста:
```
"В файле cmd/middlewares.go добавь новый middleware по аналогии с authentication.go"
"Согласно структуре CompositionRoot, создай новый handler"
"Исходя из openapi_validation.go, исправь проблему в router.go"
```

### Примеры работы с контекстом:

#### 📁 Файловый контекст:
```
Пользователь: "В файле authentication.go исправь ошибку"
AI: 1. Читаю authentication.go
    2. Анализирую структуру и паттерны
    3. Исправляю ошибку в том же стиле
    4. Проверяю связанные файлы
```

#### 🏗️ Архитектурный контекст:
```
Пользователь: "Согласно структуре CompositionRoot, добавь новый handler"
AI: 1. Читаю composition_root.go
    2. Изучаю паттерны создания handlers
    3. Создаю новый handler по тому же паттерну
    4. Обновляю связанные компоненты
```

#### 🔄 Паттерн контекст:
```
Пользователь: "По аналогии с UnitOfWork, создай новый репозиторий"
AI: 1. Читаю unit_of_work.go
    2. Анализирую паттерны репозиториев
    3. Создаю новый репозиторий в том же стиле
    4. Интегрирую в существующую архитектуру
```

### Без указания контекста:
```
"Найди все HTTP handlers которые используют UnitOfWork"
"Покажи как работает транзакционная логика в command handlers"
"Где определены доменные события для квестов?"
```

### Для исправления:
```
"В HTTP handlers есть проблема с конкурентностью UnitOfWork - 
найди все места использования и предложи решение"
```

### Для добавления:
```
"Нужно добавить валидацию JWT токенов в middleware - 
покажи текущую структуру аутентификации и предложи реализацию"
```

## 📚 Полезные команды

### Сборка и тесты:
```bash
# Сборка
go build ./...

# Все тесты
go test ./...

# Интеграционные тесты
go test -tags=integration ./tests/integration/... -v -p 1

# Линтер
golangci-lint run

# Форматирование
goimports -w .
```

### Поиск проблем:
```bash
# Поиск TODO/FIXME
grep -r "TODO\|FIXME" .

# Поиск неиспользуемых импортов
goimports -l .
```

## 🎯 Цели проекта

1. **Безопасность** - нет data races, корректная обработка ошибок
2. **Масштабируемость** - поддержка множественных запросов
3. **Тестируемость** - полное покрытие тестами
4. **Читаемость** - понятная архитектура и код
5. **Производительность** - эффективные запросы к БД

## 📝 Правила документирования изменений

### Когда создавать CHANGELOG:

**ОБЯЗАТЕЛЬНО создавать changelog при:**
1. Добавлении новых API endpoints или изменении существующих
2. Изменении OpenAPI спецификации
3. Добавлении новых слоев или компонентов архитектуры
4. Breaking changes (несовместимые изменения)
5. Интеграции с внешними сервисами
6. Изменении аутентификации/авторизации
7. Крупном рефакторинге (3+ файлов)

**НЕ создавать changelog для:**
- Мелких багфиксов
- Исправления опечаток
- Обновления комментариев
- Форматирования кода

### Структура CHANGELOG:

**Расположение:** `doc/changelog/{N}_CHANGELOG_{FEATURE_NAME}.md`

**Формат файла:**
```markdown
# {Feature Name} - Changelog

## {Emoji} Version {X.Y.Z} - {Feature Title}

### ✨ New Features
- Список новых возможностей
- Что добавлено с точки зрения пользователя

### 🔧 Technical Changes

#### **New Components**
1. **Component Name** (`path/to/file.go`)
   - Описание функционала
   - Ключевые особенности

#### **Updated Components**
1. **Component Name** (`path/to/file.go`)
   - Что изменилось
   - Зачем изменилось

### 📝 Configuration
- Новые env переменные
- Изменения в конфигурации

### 🔒 Security (если применимо)
- Изменения безопасности
- Защищенные endpoints

### 📊 Error Responses (если применимо)
- Новые коды ошибок
- Примеры ответов

### 🔄 Migration Guide
- Инструкции для пользователей
- Инструкции для разработчиков
- Breaking changes

### 📚 Dependencies (если применимо)
- Новые зависимости
- Обновленные версии

### 🧪 Testing Notes
- Что нужно протестировать
- Известные проблемы

### 🎯 Future Improvements (опционально)
- Возможные улучшения
- TODO items

### ✅ Checklist
- [ ] Список выполненных задач
- [ ] Что осталось сделать

### 🔗 Related
- Ссылки на связанные файлы
- Ссылки на документацию

---
**Breaking Change:** (если есть) Описание несовместимых изменений
```

### Пример именования:

```
doc/changelog/1_CHANGELOG_AUTH.md         - Первая фича (аутентификация)
doc/changelog/2_CHANGELOG_USER_CONTEXT.md - Вторая фича (user из контекста)
doc/changelog/3_CHANGELOG_RBAC.md         - Третья фича (роли)
```

### Workflow при больших изменениях:

1. **Анализ** → понять масштаб изменений
2. **Реализация** → внести изменения в код
3. **Тестирование** → обновить/создать тесты
4. **Документация** → создать CHANGELOG
5. **Проверка** → убедиться что все работает

---

## 📚 Правила актуальности документации

### Основные документы (`doc/`)

**Структура документации:**
```
doc/
├── INDEX.md                          # Индекс всей документации
├── ARCHITECTURE.md                   # High-level архитектура
├── API.md                           # REST API reference
├── COMPONENTS.md                    # Компоненты системы
├── CONFIGURATION.md                 # Настройки и env переменные
├── DEVELOPMENT.md                   # Руководство разработчика
├── DEPLOYMENT.md                    # Деплоймент и production
├── TESTING.md                       # Стратегия тестирования
├── ERROR_HANDLING.md                # Обработка ошибок
├── DOMAIN_EVENTS.md                 # Система событий
└── changelog/
    ├── 1_CHANGELOG_AUTH.md          # JWT Authentication
    └── 2_CHANGELOG_USER_FROM_TOKEN.md # User from Token
```

### Когда обновлять документацию:

#### **ОБЯЗАТЕЛЬНО обновить при:**

**1. API.md - обновлять при:**
- Добавлении/удалении endpoints
- Изменении request/response структур
- Изменении параметров (query, path, body)
- Изменении authentication flow
- Добавлении новых error codes
- Изменении версии API в OpenAPI

**Проверка актуальности:**
```bash
# Сравнить с OpenAPI spec
diff <(grep "operationId:" api/http/quests/v1/openapi.yaml) \
     <(grep "POST\|GET\|PATCH" doc/API.md)
```

---

**2. ARCHITECTURE.md - обновлять при:**
- Добавлении новых архитектурных слоев
- Изменении design patterns
- Добавлении новых компонентов верхнего уровня
- Изменении data flow
- Architectural Decision Records (ADR)

**Что НЕ добавлять:** Детальные описания каждого файла (это в COMPONENTS.md)

---

**3. COMPONENTS.md - обновлять при:**
- Добавлении новых domain models
- Добавлении новых handlers/use cases
- Изменении component interactions
- Добавлении новых repositories
- Изменении dependency injection

**Проверка актуальности:**
```bash
# Проверить что все handlers упомянуты
ls internal/adapters/in/http/*_handler.go | wc -l
# Сравнить с количеством в COMPONENTS.md
```

---

**4. TESTING.md - обновлять при:**
- Добавлении новых типов тестов
- Изменении test patterns
- Добавлении новых test utilities
- Изменении coverage targets
- Новых testing best practices

**Проверка актуальности:**
```bash
# Сравнить количество тестов в документации с реальным
go test ./... -v | grep "^--- PASS" | wc -l
```

---

**5. CONFIGURATION.md - обновлять при:**
- Добавлении новых env переменных
- Изменении default значений
- Добавлении новых configuration files
- Изменении config structure в Config struct

**Проверка актуальности:**
```bash
# Проверить что все env переменные документированы
grep "os.Getenv\|GetEnv" cmd/*.go internal/**/*.go | \
  cut -d'"' -f2 | sort -u
# Сравнить с CONFIGURATION.md
```

---

**6. DEVELOPMENT.md - обновлять при:**
- Изменении development workflow
- Добавлении новых команд make
- Изменении setup процедуры
- Добавлении новых dev tools
- Обновлении code style rules

---

**7. DEPLOYMENT.md - обновлять при:**
- Изменении docker-compose.yml
- Добавлении новых deployment targets
- Изменении resource requirements
- Новых environment-specific configs
- Изменении deployment процедур

---

**8. ERROR_HANDLING.md - обновлять при:**
- Добавлении новых error types
- Изменении error response format
- Добавлении новых HTTP status codes
- Изменении error handling patterns

---

**9. DOMAIN_EVENTS.md - обновлять при:**
- Добавлении новых domain events
- Изменении event structure
- Изменении event publishing strategy
- Добавлении event consumers

**Проверка актуальности:**
```bash
# Найти все события в коде
grep -r "EventName()" internal/core/domain/model/*/events.go | \
  grep "return" | cut -d'"' -f2 | sort -u
# Сравнить с каталогом событий в DOMAIN_EVENTS.md
```

---

**10. INDEX.md - обновлять при:**
- Добавлении нового документа
- Изменении структуры документации
- Добавлении новых разделов

---

### Workflow обновления документации:

```
Код изменен
    ↓
Определить затронутые документы
    ↓
Обновить соответствующие секции
    ↓
Проверить ссылки между документами
    ↓
Обновить дату "Last Updated"
    ↓
Commit вместе с кодом
```

### Checklist перед commit:

- [ ] OpenAPI spec синхронизирован с API.md
- [ ] Все env переменные документированы в CONFIGURATION.md
- [ ] Новые компоненты описаны в COMPONENTS.md
- [ ] Новые тесты упомянуты в TESTING.md
- [ ] Breaking changes задокументированы в CHANGELOG
- [ ] INDEX.md содержит все документы
- [ ] Ссылки между документами работают
- [ ] Версия обновлена где необходимо

### Автоматические проверки:

**Скрипт для проверки актуальности** (будущее):
```bash
#!/bin/bash
# scripts/check-docs.sh

# 1. Проверить что все endpoints из OpenAPI есть в API.md
# 2. Проверить что все env переменные в коде есть в CONFIGURATION.md
# 3. Проверить что количество тестов совпадает
# 4. Проверить что все .md файлы перечислены в INDEX.md
```

### Правила для AI Assistant:

**При изменении кода, ВСЕГДА проверяй:**

1. **Изменился ли OpenAPI spec?** → Обнови API.md
2. **Добавлена новая env переменная?** → Обнови CONFIGURATION.md
3. **Новый компонент/handler?** → Обнови COMPONENTS.md
4. **Новый тип теста?** → Обнови TESTING.md
5. **Breaking change?** → Создай/обнови CHANGELOG
6. **Новый error type?** → Обнови ERROR_HANDLING.md
7. **Новое domain event?** → Обнови DOMAIN_EVENTS.md

**Формат обновления:**
```markdown
### Section Title
...existing content...

#### New Feature (Added: Oct 9, 2025)
Description of new feature
...
```

### Synchronization Points:

| Код | Документ | Синхронизируемое |
|-----|----------|------------------|
| `openapi.yaml` | `API.md` | Endpoints, parameters, responses |
| `cmd/config.go` | `CONFIGURATION.md` | Environment variables |
| `internal/core/domain/` | `COMPONENTS.md` | Domain models |
| `internal/adapters/in/http/` | `COMPONENTS.md` | HTTP handlers |
| `tests/` | `TESTING.md` | Test counts, patterns |
| `**/events.go` | `DOMAIN_EVENTS.md` | Event catalog |
| `.cursorrules` | Все документы | Best practices |

---

**Помни:** Всегда анализируй перед изменением, тестируй после изменения, документируй большие изменения, **поддерживай документацию актуальной**, и следуй принципам Clean Architecture!
